import { makeStyles } from '@material-ui/styles';

export default makeStyles(() => ({
    introTextContainer: { marginBottom: '5vh' },
    titleText: { color: '#FFFFFF' },
    link: { color: '#50B83C' },
}));
